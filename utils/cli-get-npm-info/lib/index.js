"use strict";

/**
 * 获取指定包名的 npm 信息
 * @param {string} pkgName 包名
 * @param {string} registry 自选镜像地址
 */
function getNpmInfo(pkgName, registry) {
  console.log(pkgName);
}

module.exports = { getNpmInfo };
